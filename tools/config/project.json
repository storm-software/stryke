{
  "name": "tools-config",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "tools/config/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build-base": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["# Do nothing"]
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["# Do nothing"]
      }
    }
  }
}
